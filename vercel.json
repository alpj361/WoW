{
  "version": 2,
  "buildCommand": "cd frontend && rm -rf .expo .metro-cache node_modules/.cache && npm install --legacy-peer-deps && npm run build:web",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
